# TravelMesh

# how to run:

1. Create thr virtual Enviroment:

``` bash
conda create -n travel python=3.11 -y
```

2.Activate the enviroment:

```bash
conda activate travel
```

3.install the enviroments:

```bash
pip install -r requirements.txt
```

